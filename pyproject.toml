[project]
name = "healthchatagent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.120.3",
    "httpx>=0.28.1",
    "pytest>=8.4.2",
]

[dependency-groups]
dev = [
    "pytest-asyncio>=1.2.0",
]
